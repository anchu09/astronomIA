# Copia este archivo a .env y rellena OPENAI_API_KEY (y API_KEY si usas auth).
# .env no se sube al repo (está en .gitignore).

# API HTTP (opcional: si REQUIRE_API_KEY=false no hace falta poner API_KEY)
API_KEY=change-me
REQUIRE_API_KEY=false
ARTIFACT_DIR=artifacts
LOG_LEVEL=INFO

# LangSmith (opcional, para tracing)
LANGSMITH_API_KEY=
LANGSMITH_TRACING=false

# OpenAI (necesario para que el agente entienda lenguaje natural)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Opcionales
# REQUESTS_VERIFY_SSL=false
# SKYVIEW_TIMEOUT=300
